@model IEnumerable<Supermarket.Models.CategoryDiscount>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.startDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.endDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.startDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.endDate)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CategoryDiscountId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CategoryDiscountId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CategoryDiscountId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
