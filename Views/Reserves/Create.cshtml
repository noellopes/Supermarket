@model Supermarket.Models.Reserve

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>Reserve</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ReserveId" class="control-label"></label>
                <span asp-validation-for="ReserveId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="NumeroDeFunc" class="control-label">Numero de Funcionário</label>
                <input asp-for="NumeroDeFunc" class="form-control" />
                <span asp-validation-for="NumeroDeFunc" class="text-danger"></span>
            </div>
            
        </form>
    </div>
</div>

<div class="form-group">
    <input type="submit" value="Create" class="btn btn-primary" />
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
