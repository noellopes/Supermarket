@model IEnumerable<Supermarket.Models.FormationEmployee>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Formation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.NotaAtribuida)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Formation.Formation_Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.Employee_Address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.NotaAtribuida)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FormationEmployeeId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FormationEmployeeId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FormationEmployeeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
