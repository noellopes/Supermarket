@model IEnumerable<Supermarket.Models.Formation>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PontuacaoFormation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PontucaoAtribuida)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Employee)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PontuacaoFormation)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PontucaoAtribuida)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.Employee_Address)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FormationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FormationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FormationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
