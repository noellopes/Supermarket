@model Supermarket.Models.Warehouse

@{
    ViewData["Title"] = "Delete";
}
@if (ViewBag.ErrorMessage != null)
{
    <div class="alert alert-warning alert-dismissible fade show" role="alert">
        <h4 class="alert-heading">Error</h4>
        <p>@ViewBag.ErrorMessage</p>
        <a asp-action="Index">Back to List</a>
    </div>
       @if (ViewBag.SectionsAssociatedWithWarehouse != null)
    {
        <div>
            <h4>Associated Warehouse Sections</h4>
            <table class="table">
                <thead>
                    <tr>
                        <th>Description</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var warehouseSection in ViewBag.SectionsAssociatedWithWarehouse)
                    {
                        <tr>
                            <td>@warehouseSection.Description</td>
                            <td>
                                <a asp-action="Details" asp-controller="WarehouseSections" asp-route-id="@warehouseSection.WarehouseSectionId" class="btn btn-info">Details</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }

}
else{

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>Warehouse</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Adress)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Adress)
        </dd>
    </dl>
    
    <form asp-action="Delete">
        <input type="hidden" asp-for="WarehouseId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
}